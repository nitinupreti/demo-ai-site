# Python dependencies for the AEM migration agent pipeline.
#
# All commands below are relative to the repository root.
#
#   python -m venv design/site-url/scripts/.venv
#   design\site-url\scripts\.venv\Scripts\Activate.ps1        (Windows)
#   source design/site-url/scripts/.venv/bin/activate          (macOS / Linux)
#   python -m pip install -r design/site-url/scripts/requirements.txt
#
# Or run the bootstrap script, which does all of the above and also verifies the
# external tooling:
#
#   powershell -ExecutionPolicy Bypass -File design/site-url/scripts/setup.ps1   (Windows)
#   bash design/site-url/scripts/setup.sh                                        (macOS / Linux)
#
# Requires Python 3.10 or newer.
#
# ---------------------------------------------------------------------------
# Runtime dependencies
# ---------------------------------------------------------------------------
# Parses design/site-url/scripts/config/*.yaml and the fenced yaml contract
# blocks in design/site-url/prompt_new.md.
PyYAML>=6.0,<7.0

# ---------------------------------------------------------------------------
# Everything else the pipeline uses ships with Python:
#   argparse, concurrent.futures, copy, dataclasses, datetime, json, logging,
#   os, pathlib, platform, queue, re, shutil, subprocess, sys, threading, time,
#   typing, urllib, uuid
# ---------------------------------------------------------------------------

# ---------------------------------------------------------------------------
# External tooling the agents drive. These are NOT installable with pip; install
# them separately. setup.ps1 / setup.sh check for each one:
#
#   GitHub Copilot CLI   npm install -g @github/copilot   then   copilot login
#   Node.js 18+          the agents' Playwright / pixelmatch parity work
#   Java + Maven         scoped module builds and deploys
#   AEM SDK author       a running local author instance to deploy into
# ---------------------------------------------------------------------------
